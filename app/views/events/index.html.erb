<h2>All Events</h2>
<% if user_signed_in? %>
  <h3><%= link_to 'Add an event', new_event_path %></h3>
<% end %>
<% @events.each do |event| %>
  <% if event.date >= Date.today %>
    <%= render event %>
  <% end %>
<% end %>
